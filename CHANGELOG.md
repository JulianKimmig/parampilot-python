# Changelog

All notable public SDK changes are recorded here. The project follows Semantic
Versioning once a first public version is published.

## 0.1.0 — Unreleased

- Add generated Pydantic models for all 228 public programmatic API schemas.
- Add native synchronous and asynchronous clients covering all 39 operations.
- Add explicit Train, non-training Ask/Predict, typed job handles, waiters,
  progress, cancellation, pagination, downloads, and compatibility checks.
- Add resumable `add_experiments_train_and_ask` workflows whose names make
  model training explicit.
- Add deterministic backend-schema drift checks, history-isolated public-tree
  extraction, source/archive audits, and Python 3.10–3.14 CI.
- Record manifest-only candidate provenance and the bounded source-of-truth
  real-worker result covering stale inference, Train, Ask, and Predict.

No release has been published yet. This entry describes the locally validated
candidate and does not claim PyPI or GitHub availability.
